# Examples

This directory contains reference examples that are useful across projects but
are not distributed by the Copier template or `agentsmgr` defaults.

- `nb-notes/` contains Markdown note templates for `nb` coordination and
  procedure notes used by multi-agent projects.
